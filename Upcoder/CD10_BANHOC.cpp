#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    int ans = (a + 1) / 2 + (b + 1) / 2 + (c + 1) / 2; // chia từng lớp mới đúng gom lại chia có thể sai nha 
    cout << ans;
}
